import LandingLayout from './(lading)/layout';
import LandingPage from './(lading)/page';

export default function RootPage() {
  return (
    <LandingLayout>
      <LandingPage />
    </LandingLayout>
  );
}
